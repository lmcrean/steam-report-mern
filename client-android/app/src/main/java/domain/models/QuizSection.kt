/**
 * Ports from: src/context/quizConstants.js (section constants)
 * Purpose: Defines the different sections of the quiz
 */
enum class QuizSection {
    MENU,
    USERNAME,
    PERSONALITY,
    PERSONALITY_TIEBREAKER,
    SUBJECT,
    SUBJECT_TIEBREAKER,
    RESULTS,
    NETWORK_BOARD
} 